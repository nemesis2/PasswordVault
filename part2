	
<!-- Part 2 -->
  </div><!-- /entry-grid -->

  <!-- New entry form -->
  <div id="newentry" class="card" style="display:none">
    <div class="card-title"><span id="newentry-title">New Entry</span></div>

    <div class="frow">
      <span class="flabel">Name</span>
      <input class="finput" type="text" id="name" placeholder="e.g. GitHub" aria-label="Entry name"
             autocomplete="off" readonly data-clear-readonly/>
    </div>
    <div class="frow">
      <span class="flabel">URL</span>
      <input class="finput" type="text" id="url" placeholder="https://" aria-label="URL"
             autocomplete="off" readonly data-clear-readonly/>
    </div>
    <div class="frow">
      <span class="flabel">Username</span>
      <input class="finput" type="text" id="username" aria-label="Username"
             autocomplete="off" readonly data-clear-readonly/>
    </div>
    <div class="frow pw-frow">
      <span class="flabel">Password</span>
      <div class="pw-col">
        <div class="pw-row">
          <input class="finput" type="text" id="password" aria-label="Entry password" autocomplete="new-password" autocorrect="off" autocapitalize="none"/>
          <div id="pw-gen-btns">
            <button class="btn-sm" data-action="generate" data-tip="Generate a random password">Generate</button>
            <button class="btn-sm" id="copy-button" data-action="copy-current" disabled data-tip="Copy to clipboard">&#x1F4CB;&#xFE0E;</button>
          </div>
        </div>
        <div id="pw-strength-wrap" class="pw-strength-wrap" style="display:none">
          <div class="pw-segs">
            <div class="pw-seg" id="pw-seg-1"></div>
            <div class="pw-seg" id="pw-seg-2"></div>
            <div class="pw-seg" id="pw-seg-3"></div>
            <div class="pw-seg" id="pw-seg-4"></div>
            <div class="pw-seg" id="pw-seg-5"></div>
            <div class="pw-seg" id="pw-seg-6"></div>
          </div>
          <span class="pw-strength-lbl" id="pw-strength-lbl"></span>
          <span class="pw-entropy-lbl" id="pw-entropy-lbl"></span>
        </div>
      </div>
    </div>
    <div class="frow">
      <span class="flabel">2FA Token</span>
      <div class="pw-row" style="flex:1;min-width:0">
        <input class="finput" type="text" id="token" placeholder="Base32 TOTP secret" aria-label="2FA TOTP secret"
               autocomplete="off" readonly data-clear-readonly/>
        <button class="btn-sm" id="btn-scan-qr" data-action="scan-qr" data-tip="Scan a TOTP QR code">&#x1F4F7;&#xFE0E; Scan QR</button>
      </div>
    </div>
    <div class="frow" style="align-items:flex-start">
      <span class="flabel" style="padding-top:2px">Notes</span>
      <textarea class="finput fnotes" id="notes" rows="3" placeholder="Optional notes" aria-label="Notes"></textarea>
    </div>

    <div class="card-footer">
      <button class="btn btn-ghost" data-action="show-pw-settings" data-tip="Password generator settings">&#x2699;&#xFE0E; Generator</button>
      <div style="display:flex;gap:8px">
        <button class="btn btn-ghost" data-action="cancel-entry" data-tip="Cancel and discard changes">Cancel</button>
        <button class="btn btn-accent" data-action="save-entry" data-tip="Encrypt and save entry">Save Entry</button>
      </div>
    </div>
  </div>

  <!-- Password generator settings -->
  <div id="passwordSettings" class="gen-card" style="display:none">
    <div class="gen-title">Generator Settings</div>

    <div class="gen-grid">
      <div id="charset"></div>
      <input type="checkbox" id="custom"/>
      <label for="custom">Custom chars:</label>
      <input type="text" id="customchars" value="" placeholder="!@#..."
             data-check-on-input="custom"/>
      <input type="radio" name="type" id="by-length"/>
      <label for="by-length">Length</label>
      <div class="val-cell">
        <input type="number" min="1" value="16" step="1" id="length"
               data-check-on-input="by-length"/>
        <span>chars</span>
      </div>
      <input type="radio" name="type" id="by-entropy" checked/>
      <label for="by-entropy">Entropy</label>
      <div class="val-cell">
        <input type="number" min="0" value="128" step="any" id="entropy"
               data-check-on-input="by-entropy"/>
        <span>bits</span>
      </div>
    </div>

    <p id="statistics">&nbsp;</p>
    <p class="ent-note">
      <span id="crypto-getrandomvalues-entropy"></span>
      <a href="https://developer.mozilla.org/en-US/docs/Web/API/Crypto/getRandomValues" target="_blank" rel="noopener">
        <code>crypto.getRandomValues()</code></a>
    </p>
  </div>

</div><!-- /content -->

<div id="toast"></div>

<!-- Search overlay -->
<div id="search-overlay">
  <div id="search-box">
    <input type="text" id="search-input" placeholder="Search entries…" aria-label="Search entries" autocomplete="off"/>
    <div id="search-count"></div>
    <div id="search-results"></div>
  </div>
</div>

<!-- Auto-lock warning -->
<div id="lock-warning">
  <span>Locking in <span id="lock-countdown">60</span>s</span>
  <button id="lock-stay" data-action="cancel-autolock" data-tip="Stay logged in">Stay</button>
</div>

<!-- Cipher-failure warning dialog (shown on page load if any self-test fails) -->
<div id="crypto-warn-overlay">
  <div id="crypto-warn-box">
    <div id="crypto-warn-head">&#x26A0;&#xFE0E;&nbsp; Cipher Self-Test Failed</div>
    <div id="crypto-warn-body">
      <p><strong>One or more cryptographic self-tests failed.</strong> Encryption and decryption may not work correctly in this browser.</p>
      <p>Do not enter or rely on passwords until this is resolved. Try a different browser (Chrome, Edge, or Safari&nbsp;17.4+).</p>
      <p style="margin-top:12px">Failed checks:</p>
      <ul id="crypto-warn-list"></ul>
    </div>
    <div id="crypto-warn-foot">
      <button data-action="close-crypto-warn">Dismiss warning</button>
    </div>
  </div>
</div>

<!-- About modal -->
<div id="about-overlay">
  <div id="about-box">
    <div id="about-header">
      <h2>&#x2139;&#xFE0E;&nbsp; About Password Vault</h2>
      <button id="about-close" data-action="close-about" aria-label="Close">&times;</button>
    </div>
    <div id="about-body">

      <div class="about-section">
        <h3>Description</h3>
        <p>Self-hosted, client-side encrypted password manager served by a <strong>PHP-capable web server</strong>. All encryption and decryption happens entirely in your browser using the <strong>WebCrypto API</strong> — the server only stores and serves ciphertext and never sees your plaintext passwords.</p>
        <p style="margin-top:4px">Open source, released under the <a href="https://opensource.org/license/mit" target="_blank" rel="noopener">MIT License</a>.</p>
      </div>

      <div class="about-section" id="about-vault-tools">
        <h3>Vault Tools</h3>
        <p><strong>Export</strong> downloads the encrypted database exactly as stored on the server (ciphertext only). <strong>Import</strong> restores an exported <code>.lines</code> file, replacing the whole vault in one atomic save — every imported entry is decrypted with the passwords in the key fields first, so an import that does not match those passwords is refused and nothing is changed (the server also keeps a backup of the current vault before importing). <strong>Audit</strong> decrypts every entry locally and flags reused, weak, or empty passwords — names only are shown and nothing leaves this device. <strong>Change Passwords</strong> re-encrypts the whole vault under new master passwords in one atomic save. <strong>Sign</strong> stores a keyed signature (HMAC under your master passwords) over the whole vault; it is checked automatically on every unlock and refreshed after every save, so tampering, corruption, or a rolled-back copy is detected. Use the button only to accept a deliberate manual edit or backup restore as the new baseline.</p>
        <div class="vt-btns">
          <button class="btn btn-ghost" data-action="export-vault" data-tip="Download the encrypted database">&#x2B07;&#xFE0E; Export</button>
          <button class="btn btn-ghost" data-action="import-vault" data-tip="Replace the whole vault from an exported .lines file">&#x2B06;&#xFE0E; Import</button>
          <button class="btn btn-ghost" data-action="audit-vault" data-tip="Find reused / weak passwords (local only)">&#x1F50E;&#xFE0E; Audit</button>
          <button class="btn btn-ghost" data-action="toggle-chpw" data-tip="Re-encrypt the vault under new passwords">&#x1F511;&#xFE0E; Change Passwords</button>
          <button class="btn btn-ghost" data-action="resign-vault" data-tip="Sign the current contents as the new integrity baseline">&#x270D;&#xFE0E; Sign</button>
        </div>
        <div id="integrity-status" style="display:none"></div>
        <div id="audit-result" style="display:none"></div>
        <div id="import-status" style="display:none"></div>
        <div id="chpw-locked" class="vt-locked-note" style="display:none">Enter both passwords and unlock the vault first.</div>
        <div id="chpw-form" style="display:none">
          <h4 class="chpw-title">&#x1F511;&#xFE0E; Change Master Passwords</h4>
          <p class="chpw-note">Current passwords are taken from the two key fields above. Every entry is decrypted and re-encrypted in your browser, then saved in a single atomic replace — if anything fails, the vault is left untouched.</p>
          <div class="chpw-grid">
            <label for="newpw1">New password 1</label>
            <input type="password" id="newpw1" autocomplete="new-password"/>
            <label for="newpw1c">Confirm 1</label>
            <input type="password" id="newpw1c" autocomplete="new-password"/>
            <span id="chpw-match-1" class="chpw-match"></span>
            <label for="newpw2">New password 2</label>
            <input type="password" id="newpw2" autocomplete="new-password"/>
            <label for="newpw2c">Confirm 2</label>
            <input type="password" id="newpw2c" autocomplete="new-password"/>
            <span id="chpw-match-2" class="chpw-match"></span>
          </div>
          <div id="chpw-strength-bar" class="chpw-strength-bar">
            <div class="chpw-vkb-segs">
              <div class="vkb-seg" id="chpw-vkb-1"></div>
              <div class="vkb-seg" id="chpw-vkb-2"></div>
              <div class="vkb-seg" id="chpw-vkb-3"></div>
              <div class="vkb-seg" id="chpw-vkb-4"></div>
              <div class="vkb-seg" id="chpw-vkb-5"></div>
              <div class="vkb-seg" id="chpw-vkb-6"></div>
            </div>
            <span id="chpw-strength-lbl"></span>
          </div>
          <div class="chpw-foot">
            <label class="chpw-show"><input type="checkbox" id="chpw-show" data-action="toggle-chpw-show"/> Show passwords</label>
            <div class="chpw-actions">
              <button class="btn btn-accent" id="btn-chpw-go" data-action="do-chpw" disabled data-tip="Decrypt and re-encrypt every entry">Re-encrypt Vault</button>
            </div>
          </div>
          <div id="chpw-status"></div>
        </div>
      </div>

      <div class="about-section" id="about-kdf">
        <h3>&#x2699;&#xFE0E; Change KDF Parameters</h3>
        <p>Tunes the Argon2id key-derivation cost for the <strong>whole vault</strong>. Higher values make every unlock slower for you but brute-forcing far slower for an attacker. The change re-encrypts every entry in your browser under the same passwords (taken from the key fields above), then saves in a single atomic replace — if anything fails, the vault is left untouched. Memory must be 64&ndash;1024 MiB, iterations 2&ndash;10; parallelism is fixed at 1.</p>

        <div class="vt-btns">
          <button class="btn btn-ghost" data-action="toggle-kdf" data-tip="Re-encrypt the vault at a new Argon2id cost">&#x2699;&#xFE0E; Change KDF Parameters</button>
        </div>
        <div id="kdf-locked" class="vt-locked-note" style="display:none">Enter both passwords and unlock the vault first.</div>
        <div id="kdf-form" style="display:none;margin-top:10px">

          <p>Suggested settings by device class (the security column is a rough guide to how much memory-hardness an offline attacker faces):</p>
          <table class="kdf-table">
            <thead>
              <tr><th>Device class</th><th>Memory</th><th>Iter.</th><th>Security</th></tr>
            </thead>
            <tbody>
              <tr><td class="kdf-class">Small portable (phone / low-RAM)</td><td class="kdf-num">64 MiB</td><td class="kdf-num">3</td><td class="kdf-sec-low">Baseline</td></tr>
              <tr><td class="kdf-class">Laptop / typical desktop</td><td class="kdf-num">128 MiB</td><td class="kdf-num">3</td><td class="kdf-sec-mid">Good (default)</td></tr>
              <tr><td class="kdf-class">Modern desktop (16 GB+)</td><td class="kdf-num">256 MiB</td><td class="kdf-num">4</td><td class="kdf-sec-high">Strong</td></tr>
              <tr><td class="kdf-class">Large workstation (32 GB+)</td><td class="kdf-num">512 MiB</td><td class="kdf-num">4</td><td class="kdf-sec-max">Very strong</td></tr>
              <tr><td class="kdf-class">Workstation, max</td><td class="kdf-num">1024 MiB</td><td class="kdf-num">5</td><td class="kdf-sec-max">Maximum</td></tr>
            </tbody>
          </table>
          <p class="chpw-note">A high memory setting multiplied by the parallel worker count must fit in this device's RAM — raise it gradually if unlocking starts to fail on low-memory devices.</p>

          <div class="chpw-grid">
            <label for="kdf-mem">Memory (MiB)</label>
            <input type="number" id="kdf-mem" min="64" max="1024" step="64" autocomplete="off"/>
            <label for="kdf-time">Iterations</label>
            <input type="number" id="kdf-time" min="2" max="10" step="1" autocomplete="off"/>
          </div>
          <div class="chpw-foot">
            <div class="kdf-slider-wrap">
              <input type="range" id="kdf-slider" min="1" max="5" step="1" value="2" autocomplete="off" data-tip="Pick a preset from the table above"/>
              <span id="kdf-slider-lbl" class="kdf-slider-lbl"></span>
            </div>
            <div class="chpw-actions">
              <button class="btn btn-accent" id="btn-kdf-go" data-action="do-kdf" data-tip="Decrypt and re-encrypt every entry at the new cost">Re-encrypt Vault</button>
            </div>
          </div>
          <div id="kdf-status"></div>
        </div>
      </div>

      <div class="about-section" id="about-selftest">
        <h3>Runtime Self-Test</h3>
        <p style="margin-bottom:10px">Runs automatically on <strong>every page load</strong>. If any check fails a warning dialog is shown immediately. The tests also run each time this panel opens.</p>
        <div id="selftest-banner">&#x2026; Running tests</div>
        <div class="selftest-row">
          <span class="selftest-label">WebCrypto API</span>
          <span class="selftest-status loading" id="st-webcrypto">&#x2026;</span>
        </div>
        <div class="selftest-row">
          <span class="selftest-label">ChaCha20-Poly1305</span>
          <span class="selftest-status loading" id="st-chacha">&#x2026;</span>
        </div>
        <div class="selftest-row">
          <span class="selftest-label">AES-256-GCM</span>
          <span class="selftest-status loading" id="st-aes">&#x2026;</span>
        </div>
        <div class="selftest-row">
          <span class="selftest-label">Twofish-256-CTR</span>
          <span class="selftest-status loading" id="st-twofish">&#x2026;</span>
        </div>
        <div class="selftest-row">
          <span class="selftest-label">Serpent-256-CTR</span>
          <span class="selftest-status loading" id="st-serpent">&#x2026;</span>
        </div>
        <div class="selftest-row">
          <span class="selftest-label">Argon2id (WASM)</span>
          <span class="selftest-status loading" id="st-argon2">&#x2026;</span>
        </div>
        <div style="margin-top:10px;text-align:left">
          <button class="btn btn-ghost" data-action="retest-crypto">&#x21bb; Retest</button>
        </div>
      </div>
      
      <div class="about-section" id="about-shortcuts">
        <h3>Keyboard Shortcuts</h3>
        <p>Single-key shortcuts work whenever you are not typing in a field.</p>
        <table class="ks-table">
          <tr><td class="ks-key"><kbd>?</kbd></td><td class="ks-desc">Open / close search</td></tr>
          <tr><td class="ks-key"><kbd>N</kbd></td><td class="ks-desc">New entry (when the &#xff0b; New button is enabled)</td></tr>
          <tr><td class="ks-key"><kbd>E</kbd></td><td class="ks-desc">Edit the selected entry (when one is displayed)</td></tr>
          <tr><td class="ks-key"><kbd>Shift</kbd> <kbd>D</kbd></td><td class="ks-desc">Delete the selected entry, with confirmation (when one is displayed)</td></tr>
          <tr><td class="ks-key"><kbd>C</kbd></td><td class="ks-desc">Clear the displayed entry</td></tr>
          <tr><td class="ks-key"><kbd>A</kbd> / <kbd>I</kbd></td><td class="ks-desc">Open this About panel</td></tr>
          <tr><td class="ks-key"><kbd>X</kbd></td><td class="ks-desc">Lock the vault</td></tr>
          <tr><td class="ks-key"><kbd>Esc</kbd></td><td class="ks-desc">Close dialog / clear displayed entry</td></tr>
          <tr><td class="ks-key"><kbd>Esc</kbd> <kbd>Esc</kbd></td><td class="ks-desc">Lock the vault (press twice quickly)</td></tr>
        </table>
      </div>      

      <div class="about-section" id="about-vault-signing">
        <h3>Vault Signing</h3>
        <p>Every save automatically signs the entire record set with a <strong>keyed HMAC</strong> that only a password holder can produce. The server stores the signature opaquely — it has no access to your passwords and can never forge or verify it. On every unlock, the browser recomputes the signature over the records it received and compares. Any discrepancy — an entry tampered with, silently deleted, or corrupted — is shown as a red failure badge rather than passing unnoticed.</p>
        <p style="margin-top:8px"><strong>Signature construction</strong></p>
        <table class="cr-table" style="margin-top:4px">
          <tr><td class="cr-name">Vault key</td><td class="cr-desc">HKDF-SHA-256 over <code>Argon2id(pw1, salt1) ‖ Argon2id(pw2, salt2)</code>, info <code>v6|manifest|hmac</code> — independent of every per-record key</td></tr>
          <tr><td class="cr-name">Signed data</td><td class="cr-desc">Header fields (format, salts, revision, timestamp) + every record joined in canonical server order</td></tr>
          <tr><td class="cr-name">Algorithm</td><td class="cr-desc">HMAC-SHA-256 via WebCrypto</td></tr>
          <tr><td class="cr-name">Revision</td><td class="cr-desc">Monotonic counter incremented on every sign; each device remembers the highest revision it has seen, so a validly signed but older copy of the vault raises a <strong>rollback</strong> alert</td></tr>
        </table>
        <p style="margin-top:8px"><strong>Manifest format</strong> (stored server-side, embedded in the served page):</p>
        <pre style="font-size:11px;margin-top:4px;padding:8px;background:var(--bg);border:1px solid var(--border);border-radius:var(--r);overflow-x:auto">vm1 | salt1 | salt2 | revision | timestamp | hmac</pre>
        <p style="margin-top:8px"><strong>Limits:</strong> this is tamper <em>detection</em> for the data file. It guards against corruption, botched restores, and manual-edit mistakes. It cannot defend against an adversary who can also rewrite the served JavaScript — no in-band scheme can.</p>
      </div>

      <div class="about-section">
        <h3>Record Formats</h3>
        <p><strong>Record format (v6)&thinsp;—&thinsp;doubly-encrypted name:</strong></p>
        <p><code>encName | v6 | recSalt1 | recSalt2 | nameNonce1 | nameNonce2 | iv1 | nonce2 | nonce3 | nonce4 | ciphertext</code></p>
        <p style="margin-top:4px"><code>recSalt1</code> and <code>recSalt2</code> are the two Argon2id salts (one per master password); every per-cipher key is HKDF-derived from the resulting master keys, so the record carries only nonces, not per-layer salts. The entry name is double-encrypted: <code>AES-256-GCM</code> (password&nbsp;1) as the inner layer, wrapped by <code>ChaCha20-Poly1305</code> (password&nbsp;2) as the outer layer. Both passwords are required to reveal the name. Entry buttons are hidden on page load; they appear automatically when both key fields contain a value and the secondary key field loses focus.</p>
        <p style="margin-top:4px">Records are stored in a flat file, one per line, sorted alphabetically. The line index is appended at runtime for delete operations.</p>
      </div>

      <div class="about-section">
        <h3>Key Derivation</h3>
        <p>Each record derives <strong>two 256-bit master keys</strong> with memory-hard <code>Argon2id</code> (<strong>m&nbsp;=&nbsp;128&nbsp;MiB, t&nbsp;=&nbsp;3, p&nbsp;=&nbsp;1</strong>) &mdash; one per master password, each over a <strong>32-byte random salt</strong> generated fresh for every save. <code>HKDF-SHA-256</code> then expands each master key into the independent per-cipher subkeys (distinct <code>info</code> labels), so the expensive memory-hard step runs only <strong>twice per record</strong> while every layer still gets its own key.</p>
        <p style="margin-top:6px">Revealing many entry names at once would otherwise serialise the memory-hard hashes on the main thread (the WASM runs synchronously behind an internal mutex). To keep the UI responsive, each derivation is dispatched to a pool of dedicated <strong>Web Workers</strong> &mdash; one WebAssembly instance each &mdash; so peak memory stays bounded at pool&nbsp;&times;&nbsp;128&nbsp;MiB. The pool is sized at the <strong>reported CPU core count</strong> &mdash; capped at <strong>16</strong> on desktop and <strong>2</strong> on phones/tablets (less RAM, thermal throttling) &mdash; and further bounded by available device memory so the 128&nbsp;MiB heaps stay within roughly half of RAM. It is created lazily and torn down on every lock, reclaiming its WASM heaps and dropping residual password bytes. A derivation that fails transiently (memory pressure, a worker crash) is <strong>retried</strong> &mdash; falling back to the main thread between attempts &mdash; so an entry is never silently dropped from the grid; if Workers are unavailable entirely, derivation runs on the main thread.</p>
      </div>

      <div class="about-section">
        <h3>Encryption Pipeline</h3>
        <div class="about-chain">
          <span class="about-chip">Plaintext&nbsp;JSON</span>
          <span>&#x2192;</span>
          <span class="about-chip">Pad&nbsp;to&nbsp;256&nbsp;B</span>
          <span>&#x2192;</span>
          <span class="about-chip">ChaCha20-Poly1305</span>
          <span>&#x2192;</span>
          <span class="about-chip">AES-256-GCM</span>
          <span>&#x2192;</span>
          <span class="about-chip">Twofish-256-CTR</span>
          <span>&#x2192;</span>
          <span class="about-chip">Serpent-256-CTR</span>
          <span>&#x2192;</span>
          <span class="about-chip">Stored&nbsp;hex</span>
        </div>
        <div class="crypto-row">
          <span class="crypto-layer">Layer 1</span>
          <span><code>ChaCha20-Poly1305</code> &mdash; AEAD &mdash; password&nbsp;1, 12-byte nonce</span>
        </div>
        <div class="crypto-row">
          <span class="crypto-layer">Layer 2</span>
          <span><code>AES-256-GCM</code> &mdash; AEAD &mdash; password&nbsp;1, 12-byte IV</span>
        </div>
        <div class="crypto-row">
          <span class="crypto-layer">Layer 3</span>
          <span><code>Twofish-256-CTR</code> &mdash; password&nbsp;2, 16-byte nonce (unauthenticated; relies on inner AEAD layers for integrity)</span>
        </div>
        <div class="crypto-row">
          <span class="crypto-layer">Layer 4</span>
          <span><code>Serpent-256-CTR</code> &mdash; password&nbsp;2, 16-byte nonce (unauthenticated; relies on inner AEAD layers for integrity)</span>
        </div>
        <p style="margin-top:6px">The two inner AEAD layers authenticate the ciphertext. A wrong password or tampered data causes immediate authentication failure before any plaintext is revealed.</p>
        <p style="margin-top:6px"><strong>Length-hiding padding.</strong> Before the first layer, the plaintext JSON is padded up to the next <strong>256-byte</strong> boundary with trailing spaces. The stored ciphertext length therefore reveals only which 256-byte bucket an entry falls into &mdash; not the true length of a password or note &mdash; so someone who obtains the raw database cannot tell a short password from a long one. The padding is valid trailing JSON whitespace, so it is discarded automatically on decrypt with no separate un-pad step.</p>
      </div>

      <div class="about-section">
        <h3>Two-Password Design</h3>
        <p>Layers 1 and 2 use <strong>password 1</strong>; layers 3 and 4 use <strong>password 2</strong>. Both passwords are required to decrypt any entry. Either password alone reveals nothing about the stored data.</p>
        <p style="margin-top:6px">The entry <strong>name</strong> is protected by both passwords as well: the inner <code>AES-256-GCM</code> layer uses password&nbsp;1; the outer <code>ChaCha20-Poly1305</code> layer uses password&nbsp;2. An attacker who compromises one password cannot reveal entry names or payload.</p>
      </div>

      <div class="about-section">
        <h3>Cipher Rationale</h3>
        <p>The four layers span <strong>three independent cipher families</strong> from different design lineages. An attacker breaking the stored ciphertext must defeat all four simultaneously &mdash; breaking AES does not weaken ChaCha20, Twofish, or Serpent. Each cipher was chosen to complement the others' failure modes.</p>
        <table class="cr-table">
          <tr>
            <td class="cr-name"><code>ChaCha20-Poly1305</code></td>
            <td class="cr-tag"><span class="cr-aead">AEAD</span></td>
            <td class="cr-desc">Stream cipher designed by Daniel J. Bernstein. No S-box lookups &mdash; constant-time by construction, immune to cache-timing attacks. Adopted in TLS&nbsp;1.3, SSH, and WireGuard as the standard software-path cipher. The Poly1305 tag authenticates the ciphertext; a wrong password or tampered byte causes immediate rejection before any plaintext is produced.</td>
          </tr>
          <tr>
            <td class="cr-name"><code>AES-256-GCM</code></td>
            <td class="cr-tag"><span class="cr-aead">AEAD</span></td>
            <td class="cr-desc">The global standard block cipher, NIST-standardised and FIPS&nbsp;140-2 compliant. Hardware-accelerated (AES-NI) on all modern CPUs. The most widely analysed cipher in existence. GCM adds a Galois-field authentication tag, giving full authenticated encryption. Used here as the second AEAD layer so two independent authentication checks must both pass.</td>
          </tr>
          <tr>
            <td class="cr-name"><code>Twofish-256-CTR</code></td>
            <td class="cr-tag"><span class="cr-ctr">CTR</span></td>
            <td class="cr-desc">AES finalist (top 5), designed by Bruce Schneier, John Kelsey, Doug Whiting, David Wagner, Chris Hall, and Niels Ferguson. Key-dependent S-boxes and a Feistel structure entirely different from AES&rsquo;s SubBytes/ShiftRows/MixColumns design. Adds a third independent key under password&nbsp;2, so an attacker who knows password&nbsp;1 still cannot read the plaintext.</td>
          </tr>
          <tr>
            <td class="cr-name"><code>Serpent-256-CTR</code></td>
            <td class="cr-tag"><span class="cr-ctr">CTR</span></td>
            <td class="cr-desc">AES finalist that came <strong>second</strong> in the panel vote, designed by Ross Anderson, Eli Biham, and Lars Knudsen. Chosen by the designers specifically for its conservative security margin: 32 rounds (vs AES&rsquo;s 14), with a simple bitslice structure that has resisted cryptanalysis for over 25 years. The outermost layer &mdash; any weakness in layers 1&ndash;3 is still protected here.</td>
          </tr>
        </table>
        <p style="margin-top:10px"><strong>Argon2id &mdash; making brute-force infeasible.</strong> Before any cipher sees a key, each password is passed through Argon2id (PHC winner, 2015) with <strong>128&nbsp;MiB of RAM and 3 passes</strong>. This means every single password guess an attacker makes requires 128&nbsp;MiB of memory and significant CPU time. A GPU farm that can test billions of bcrypt hashes per second is reduced to a few thousand Argon2id guesses per second per card. Combined with a strong, unique second password, this raises the cost of an offline brute-force attack to be computationally prohibitive.</p>
        <p style="margin-top:6px">The overall result: even if an attacker obtains the raw ciphertext file, they face four independent cipher layers with independent HKDF-derived keys, two independent AEAD authentication checks, and a memory-hard KDF that serialises every guess into a 128&nbsp;MiB, multi-pass operation &mdash; all of which must be overcome before a single password is revealed.</p>
      </div>

      <div class="about-section">
        <h3>TOTP (2FA)</h3>
        <p><code>HMAC-SHA-1</code> &mdash; RFC&nbsp;6238 &mdash; 30-second window &mdash; 6-digit codes.</p>
        <p>Secrets are stored as Base32 in the <code>token</code> field, encrypted with the rest of the entry fields.</p>
        <p style="margin-top:4px">The <strong>📷 Scan QR</strong> button in the new-entry form uses the browser's <code>BarcodeDetector</code> API to read a TOTP QR code from a photo or image file. The <code>secret=</code> parameter is extracted from the <code>otpauth://</code> URI; the issuer and account name are used to pre-fill the Name and Username fields if they are empty. Requires Chrome&nbsp;83+, Edge&nbsp;83+, or Safari&nbsp;17.4+.</p>
      </div>

      <div class="about-section">
        <h3>Password Strength</h3>
        <p>While typing a password in the new-entry form, a six-segment bar and label estimate strength from the character classes present (lowercase, uppercase, digits, symbols) and the password length. Entropy is reported in bits.</p>
        <table style="width:100%;margin-top:6px;border-collapse:collapse;font-size:12px">
          <tr><td style="color:#ff5c5c;font-family:var(--mono);padding:1px 8px 1px 0">Weak</td><td style="color:var(--text-2)">&lt; 40 bits</td></tr>
          <tr><td style="color:#f5a623;font-family:var(--mono);padding:1px 8px 1px 0">Fair</td><td style="color:var(--text-2)">40 – 79 bits</td></tr>
          <tr><td style="color:#3fcf8e;font-family:var(--mono);padding:1px 8px 1px 0">Strong</td><td style="color:var(--text-2)">80 – 119 bits</td></tr>
          <tr><td style="color:#4d8eff;font-family:var(--mono);padding:1px 8px 1px 0">Very Strong</td><td style="color:var(--text-2)">120 – 159 bits</td></tr>
          <tr><td style="color:#a878ff;font-family:var(--mono);padding:1px 8px 1px 0">Exceptional</td><td style="color:var(--text-2)">160 – 199 bits</td></tr>
          <tr><td style="color:#ff5cc8;font-family:var(--mono);padding:1px 8px 1px 0">Paranoid</td><td style="color:var(--text-2)">&ge; 200 bits</td></tr>
        </table>
      </div>

      <div class="about-section">
        <h3>Key Caching</h3>
        <p>Argon2id master keys are memoised per <code>(password,&nbsp;salt)</code> pair for the duration of the session, so each entry incurs only its two memory-hard derivations once. Clicking an entry after reveal-all reuses the cached master keys (no further Argon2id work). The cache is wiped on auto-lock or manual clear.</p>
      </div>

      <div class="about-section">
        <h3>Auto-lock</h3>
        <p>After <strong>5 minutes</strong> of inactivity a 60-second warning appears. If not dismissed, all key fields and cached keys are cleared from memory.</p>
      </div>

      <div class="about-section">
        <h3>Libraries &amp; Attribution</h3>
        <p>Cipher primitives are third-party open-source, bundled <strong>inline</strong> &mdash; no runtime network requests; Argon2id WASM is embedded as base64. Each is used under its license, upstream notices retained.</p>
        <table class="lib-table">
          <tr>
            <td class="lt-pkg"><code>@noble/ciphers</code></td>
            <td class="lt-use">MIT<br><span class="lt-ver">Paul Miller</span></td>
            <td class="lt-info">ChaCha20-Poly1305<br><a href="https://github.com/paulmillr/noble-ciphers" target="_blank" rel="noopener">github.com/paulmillr/noble-ciphers</a></td>
          </tr>
          <tr>
            <td class="lt-pkg"><code>twofish-ts</code><br><span class="lt-ver">v1.0.2</span></td>
            <td class="lt-use">MIT<br><span class="lt-ver">Logan R. Kearsley</span></td>
            <td class="lt-info">Twofish-256<br><a href="https://github.com/gliese1337/twofish" target="_blank" rel="noopener">github.com/gliese1337/twofish</a></td>
          </tr>
          <tr>
            <td class="lt-pkg"><code>hash-wasm</code><br><span class="lt-ver">v4.12.0</span></td>
            <td class="lt-use">MIT<br><span class="lt-ver">Dani Bir&oacute;</span></td>
            <td class="lt-info">Argon2id (WebAssembly)<br><a href="https://github.com/Daninet/hash-wasm" target="_blank" rel="noopener">github.com/Daninet/hash-wasm</a></td>
          </tr>
          <tr>
            <td class="lt-pkg"><code>Serpent-256</code></td>
            <td class="lt-use">Public domain</td>
            <td class="lt-info">In-tree bitslice cipher (CTR), not from npm. Verified against <a href="https://www.bouncycastle.org/" target="_blank" rel="noopener">Bouncy Castle</a> 256-bit vectors; algorithm by <a href="https://www.cl.cam.ac.uk/~rja14/serpent.html" target="_blank" rel="noopener">Anderson, Biham &amp; Knudsen</a>.</td>
          </tr>
          <tr>
            <td class="lt-pkg"><code>esbuild</code></td>
            <td class="lt-use">MIT<br><span class="lt-ver">Evan Wallace</span></td>
            <td class="lt-info">Build tooling only &mdash; not shipped to the browser.<br><a href="https://github.com/evanw/esbuild" target="_blank" rel="noopener">github.com/evanw/esbuild</a></td>
          </tr>
        </table>
      </div>

      <div class="about-section">
        <h3>Standards Alignment</h3>
        <div class="standards-grid">
          <span class="sg-name">Argon2 (RFC 9106)</span>
          <span class="sg-met">Met</span>
          <span class="sg-desc">Argon2id key derivation &mdash; m&nbsp;=&nbsp;128&nbsp;MiB, t&nbsp;=&nbsp;3, p&nbsp;=&nbsp;1 (exceeds the RFC&nbsp;9106 second-recommended option), then HKDF-SHA-256 subkeys</span>

          <span class="sg-name">RFC 6238</span>
          <span class="sg-met">Met</span>
          <span class="sg-desc">TOTP &mdash; 30-second window, 6-digit codes</span>

          <span class="sg-name">OWASP ASVS L1/L2</span>
          <span class="sg-met">Met</span>
          <span class="sg-desc">Client-side crypto, CSRF protection, Basic-Auth rate limiting, full header suite (CSP, HSTS, COOP/CORP, Permissions-Policy)</span>

          <span class="sg-name">OWASP ASVS L3</span>
          <span class="sg-partial">Partial</span>
          <span class="sg-desc"><code>style-src 'unsafe-inline'</code> still required by inline <code>style</code> attributes; <code>script-src</code> is <code>'self' 'wasm-unsafe-eval'</code> (the latter only to compile the Argon2id WASM). Key derivation is memory-hard Argon2id</span>

          <span class="sg-name">FIPS 140</span>
          <span class="sg-partial">Partial</span>
          <span class="sg-desc">AES-256-GCM and HKDF-SHA-256 are approved; ChaCha20, Twofish, Serpent and Argon2id are not</span>
        </div>
      </div>

      <div class="about-section">
        <h3>Hello There</h3>
        <p>You read this far and made it all the way to the end?<br />As a reward: stand up, take a 5 minute break and go stretch.</p>
      </div>

    </div>
  </div>
</div>

</body>
</html>
